const serverHost = "localhost";
const serverPort = 8080;
const protocol = "http"

window.CONSTS = {
    host: serverHost,
    port: serverPort,
    serverUrl: `${protocol}://${serverHost}:${serverPort}`,
}